{
  "name": "img-seq-pack",
  "private": true,
  "version": "0.2.0-dev",
  "type": "module",
  "scripts": {
    "dev": "yarn build-template && vite",
    "build": "yarn build-template && vite build",
    "build-template": "tsc && vite build --config vite-template.config.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.3.9",
    "vite-plugin-singlefile": "^0.13.5"
  },
  "dependencies": {
    "hash.js": "^1.1.7",
    "tga-js": "^1.1.1"
  }
}
